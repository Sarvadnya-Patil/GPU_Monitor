# Reference notes, deliberately kept out of git (see its own header)
GPU_SETUP_CONTEXT.md

# Python
__pycache__/
*.pyc
.venv/
venv/

# Env files with secrets
.env

# Server runtime data
server/data/*.db
server/data/*.db-*
server/data/backups/*
!server/data/backups/.gitkeep

# Node / frontend
frontend/node_modules/
frontend/dist/

# Built dashboard assets (generated by `npm run build`, served by FastAPI)
server/static/*
!server/static/.gitkeep
